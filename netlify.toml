[build]
  command = "npm run build"
  publish = "dist"

[[edge_functions]]
  path = "/api/chat"
  function = "chat"

[dev]
  command = "npm run dev"
  targetPort = 8080
  port = 8888
  publish = "dist"
